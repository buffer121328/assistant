# Prompts

Prompt templates belong here when they are reused by runtime code.

This directory is intentionally separate from `backend/resources/skillpacks`: prompts are model
instructions or response templates, while skills are user-facing capability
packages with their own `SKILL.md` metadata.
